<!-- Modal -->
<div class="modal fade" id="add-modal-pengembalian" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Add Pengembalian Buku</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
            </div>

            <form action="/admin/pengembalian" method="post">
                <div class="modal-body">
                    <div class="form-group">
                        <label for="name">Tanggal Pengembalian</label>
                        <input type="date" class="form-control" name="dateReturn" id="return" autocomplete="off" required>
                    </div>
                    <div class="form-group">
                        <label for="name">Nama Buku</label>
                        <select class="form-control" name="booksId">
                          <option value="">--- Nama Buku ---</option>
                          <% for(let i = 0; i< book.length; i++){ %>
                          <option value="<%= book[i].id %>"><%= book[i].title %></option>
                          <%}%>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="name">Peminjam</label>
                        <select class="form-control" name="memberId">
                          <option value="">--- Peminjam ---</option>
                          <% for(let i = 0; i< member.length; i++){ %>
                          <option value="<%= member[i].id %>"><%= member[i].name %></option>
                          <%}%>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="name">Petugas</label>
                        <select class="form-control" name="officerId">
                          <option value="">--- Petugas ---</option>
                          <% for(let i = 0; i< officer.length; i++){ %>
                          <option value="<%= officer[i].id %>"><%= officer[i].name %></option>
                          <%}%>
                        </select>
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Tambah Data</button>
                </div>
            </form>
        </div>
    </div>
</div>
</div>